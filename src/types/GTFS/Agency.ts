export type GTFSAgency = {
  // CSV Headers: agency_id,agency_name,agency_url,agency_timezone,agency_phone
  agency_id: string;
  agency_name: string;
  agency_url: string;
  agency_timezone: string;
  agency_phone: string;
};
